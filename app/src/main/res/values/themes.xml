<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.APPPLAYLISTMAKER" parent="Theme.Material3.DayNight.NoActionBar">

        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.APPPLAYLISTMAKER" parent="Base.Theme.APPPLAYLISTMAKER" >
        <item name="buttonStyle">@android:color/white</item>
        <item name="android:fontFamily">@font/yandex_sansdisplay_regular</item>
    </style>
</resources>